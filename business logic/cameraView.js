// if android/ios

// Start camera
// stream two other cameras streams using device mac/identifiers

// if web, show all cameras in a panaroma/ continuous mode

// signal succesful init.

// on start, call signal for gps readings
// create pubsub, everyone pubs, server subs
// most common gps elected. Share every 200ms. loop

// shoot every 5 meters in a straight line

// shoot more frequently in corners

// SHOOT SUBROUTINE
// Shoot image, attach date, number, gps location, compass heading
